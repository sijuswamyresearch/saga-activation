# Read the Docs configuration file
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

# This is the block that was missing! It tells RTD where conf.py is.
sphinx:
  builder: html
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt